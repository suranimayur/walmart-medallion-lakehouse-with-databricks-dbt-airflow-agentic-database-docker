packages:
  # dbt utility macros package.
  # NOTE: currently unused by any model/test/macro — remove this block (and run
  # `dbt deps` again) if you do not plan to use dbt_utils helpers such as
  # unique_combination_of_columns or equality tests.
  - package: dbt-labs/dbt_utils
    version: [">=1.0.0", "<2.0.0"]
